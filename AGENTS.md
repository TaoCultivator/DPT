1. github cli发布由于沙箱权限问题需要提权，hosts.yml 里没有明文 token（正常情况）。在 Windows 上，gh 通常把 token 存在 Windows 凭据管理器里。
2. 参数数值的计算千万别改；涉及提取、测量、积分、斜率、时间窗等计算逻辑时，除非用户明确要求，否则只允许改展示层、交互层或样式。
3. 不要擅自新增弹窗提示、悬浮提示（ToolTip）或一次性提示；如确实需要，必须先说明用途并获得用户明确同意。
4. GitHub Release 资产命名必须沿用历史规则：上传的 Windows exe 统一命名为 `DPT_Windows_vX.Y.Z.exe`。本地 PyInstaller 可继续生成中文文件名的 exe，但发布前应复制/重命名为该资产名再上传；不要改成 `DPT_Extractor_*` 或其他临时命名，也不要上传中文名导致 GitHub 资产名乱码。
5. 波形通道标识必须贴该通道的 0 刻度位置显示；不要把通道标识锚到波形曲线、损耗窗口曲线或其他非 0 刻度位置。
6. 遇到波形初始加载位置/通道标识看起来歪、但手动拖动一下标识后恢复正常的问题，优先排查 Qt/PyQtGraph 初始布局时序：在 ViewBox `sigResized`、窗口 `resizeEvent`/`showEvent`、波形加载完成后的延迟帧里强制刷新可见曲线并重新定位 0 刻度通道标识；不要用改积分/测量/提取结果或把标识锚到损耗曲线窗口来规避。
7. 遇到拖动通道标识移动波形时卡顿，优先优化鼠标移动热路径：拖动中只刷新当前通道曲线和当前通道 0 标识，鼠标释放后再补 overview、坐标轴、读数、图例等重刷新；GPU/OpenGL 只能作为可选绘图后端开关，NPU 不适合这种 Qt 波形绘制热路径。
8. 偏移测量的定义必须以泰克官网资料为准，不允许凭印象补定义。当前依据：Tektronix `4, 5, 6 Series MSO Programmer Manual`（Part Number `077189803`，Release Date `2026-03-11`，`MEASUrement:ADDMEAS` 测量类型说明，https://www.tek.com/en/manual/oscilloscope/4-5-6-series-mso-programmer-manual-5-series-mso-low-profile 和 PDF https://download.tek.com/manual/4-5-6-Series-MSO_Programmer_077189803.pdf），以及 Tektronix TBS2000 自动测量教学页（RMS/Mean/Area 定义说明，https://www.tek.com/en/video/how-to/how-to-make-rms-mean-and-area-measurements-using-automatic-functions-on-a-tbs2000-oscilloscope）。按该资料，偏移测量含义为：`Maximum` 是选定范围内最大数据点；`Minimum` 是选定范围内最小数据点；`Peak-to-Peak` 是 `Maximum - Minimum`；`Mean` 是数据点算术平均值；`RMS` 是数据点真均方根；`AC RMS` 是相对于 `Mean` 的真均方根；`Area` 是对数据点积分，0 轴以上面积为正、0 轴以下面积为负；`Top` 是波形中点以上最常见的数据值；`Base` 是波形中点以下最常见的数据值；`Amplitude` 是 `Top` 与 `Base` 的差值；`Positive Overshoot` 是 `(Maximum - Top) / Amplitude * 100%`；`Negative Overshoot` 是 `(Base - Minimum) / Amplitude * 100%`。`全波形`、`屏幕`、`光标` 只改变取样时间范围，不改变上述定义；若实现中用分位数等轻量方法近似 `Top/Base` 的“最常见值”，必须在代码/说明中称为近似，不能宣称完全复刻泰克 histogram 算法，除非已按官方算法实现并用可信样例验证。
9. 偏移测量表的“分区”列只显示短通道名（如 `Ch 1`、`Math 1`），不要带示波器 Label 或逻辑标签；该表正文使用普通字重，文字必须完整显示，并保留清晰的行列分割线。
10. TSS 双脉冲重点复核样例：`示例文件/tss格式/KSU2577/SSM1R7PB12B3DTFMMSPP25M4CF0016/SSS/RT/tss/UH_750V_1050A_000.tss` 与 `.../UL_750V_1050A_000.tss` 是回归基准样例。2026-06-16 根据专业损耗口径修复 Eoff/Eon/Err 右侧光标后，旧“第一次穿平台/固定尾窗/最后显著拖尾”快照不再作为最终值。后续优化其他工况时，必须把这两个文件作为重点保护样例；除非用户明确要求重标定，否则不得改动会导致这些文件的脉冲识别、分段、平台电平或提取数值无依据漂移的计算逻辑。当前口径和快照如下：
    - 两个文件都必须识别为双脉冲，`detected_pulse_count=2`，关断取第 1 脉冲，开通/反向恢复取第 2 脉冲，不能退化成单脉冲或把开通窗落到错误平台。
    - UH RT 分段：`turn_off=13.831680~15.633040us`，`turn_on=18.197440~19.158720us`，`reverse_recovery=18.582720~18.676800us`。UL RT 分段：`turn_off=14.725040~16.384560us`，`turn_on=19.184640~20.158320us`，`reverse_recovery=19.550080~19.635760us`。
    - Eoff 口径：`Ha` 是 Vce 抬升前导通平台，`A` 是 Vce 主上升沿穿 `Ha` 的真实交点；`Hb` 是带符号 Ic 回落平台，`B` 是本次关断过程中 Ic 经过主下降后、局部可见阻尼振荡首次达到低幅稳定标准时与 `Hb` 的真实交点；积分用原始 `Vce * Ic`。不得使用主沿前噪声穿越、第一次刚碰平台就截断、稳定后的长尾小噪声/后续 LC 尾巴、最后显著拖尾或段界点作为 A/B。UH 候选快照：`A=14.518515us`，`B=14.942261us`，`Ha=11.59375V`，`Hb=40.558667A`，`Eoff=93.896759mJ`。UL 候选快照：`A=15.460102us`，`B=15.860224us`，`Ha=7.78125V`，`Hb=-23.041583A`，`Eoff=67.267305mJ`。
    - Eon 口径：`Ha` 是带符号 Ic 抬升前平台，`A` 是 Ic 主上升沿穿 `Ha` 的真实交点；`Hb` 是 Vce 回落后导通平台，`B` 是本次开通过程中 Vce 经过主下降后、局部可见阻尼振荡首次达到低幅稳定标准时与 `Hb` 的真实交点；积分用原始 `Vce * Ic`。`Hb(Vce)` 不得因为手动拖动竖向光标后的联动显示值而重标定，平台线仍按自动平台算法取值。不得使用主沿前噪声穿越、第一次刚碰平台就截断、稳定后的长尾小噪声/后续 LC 尾巴、最后显著拖尾或段界点作为 A/B。UH 候选快照：`A=18.433584us`，`B=18.770400us`，`Ha=32.140625A`，`Hb=4.09375V`，`Eon=68.330365mJ`。UL 候选快照：`A=19.410792us`，`B=19.687082us`，`Ha=-32.15625A`，`Hb=7.78125V`，`Eon=51.634386mJ`。
    - Err 口径：`Ha` 是 IRM 主峰后主要大振荡结束、进入本次恢复过程稳定区后的 Irr base 有效值，取首次达到低幅稳定标准的本地稳定窗内 `(max+min)/2`；`A` 是这条 `Ha` 横线与 Irr 曲线在稳定区入口附近的真实交点，积分也必须使用同一个 A，不得用拟合点或旧首个下降点代替；`Hb` 是第二脉冲开通过程/反向恢复段内，对管电压 Vd 主上升沿前的本地 base，不是全局 base，也不是第一脉冲关断过程 base；`B` 唯一口径是同一段 Vd 曲线与 `Hb` 横线在主上升沿上的真实插值交点；积分窗口取 A/B 之间，积分用 `abs(Vd) * abs(Irr)`。不得再使用主沿前 0V 附近噪声上穿点、主峰前最后一次上穿点、固定 `400~800ns` 尾窗、拟合点、段界点、最后更小尾窗或旧兼容分支作为 A/B。UH 快照：`A=18.930560us`，`B=18.615986us`，`Ha=29.31250A`，`Hb=7.78125V`，`Err=9.529396mJ`。UL 快照：`A=19.854689us`，`B=19.571331us`，`Ha=-38.56250A`，`Hb=4.09375V`，`Err=9.369227mJ`。
    - 开通电流/开通 di/dt 的 `Ha` 必须共用同一套 Ic 抬升后平台逻辑并保持一致。UH：`Hb=32.140625A`，`Ha=1036.125A`，`A=18.414384us`，`B=19.001280us`。UL：`Hb=-32.15625A`，`Ha=1038.390625A`，`A=19.384081us`，`B=19.968320us`。
    - 开通 `Delta Vce`/`Ls_on` 的下点是 Vce 主下降过程中的拐点/平台点，不允许改成全段最小值或损耗窗口点。UH：`Vce_on_max=763.59375V`，拐点 `18.632640us @ 446.488971V`，`Delta Vce=309.698529V`，`Ls_on=47.174095nH`。UL：`Vce_on_max=746.21875V`，拐点 `19.598880us @ 413.938113V`，`Delta Vce=330.249387V`，`Ls_on=41.237653nH`。
    - 关键结果快照：UH `Vdc=756.03125V`、`Idc=1051.25A`、`Delta Vce off=337.21875V`、`dv/dt off=7.594292V/ns`、`di/dt off=10.622707A/ns`、`Eoff=93.896759mJ`、`dv/dt on=2.596768V/ns`、`di/dt on=6.565013A/ns`、`Eon=68.330365mJ`、`Irr=173.90625A`、`Trr=32.815655ns`、`Vrr=985.03125V`、`dv/dt rr=12.970562V/ns`、`di/dt rr=13.737728A/ns`、`Err=9.529396mJ`。UL `Vdc=744.0625V`、`Idc=1054.25A`、`Delta Vce off=345.25V`、`dv/dt off=10.957675V/ns`、`di/dt off=12.435164A/ns`、`Eoff=67.267305mJ`、`dv/dt on=3.004423V/ns`、`di/dt on=8.008443A/ns`、`Eon=51.634386mJ`、`Irr=113.6875A`、`Trr=32.847202ns`、`Vrr=1029.03125V`、`dv/dt rr=14.146104V/ns`、`di/dt rr=10.831239A/ns`、`Err=9.369227mJ`。
    - 涉及 `PulseDetector`、`Segmenter`、`metrics/plateau_level.py`、`metrics/iec_windows.py`、`metrics/slopes.py`、`pipeline/extract.py` 或 GUI 卡尺默认落点的修改，都必须先用上述两个黄金样例和全部示例 TSS 进行回归；若为兼容其他工况需要加入分支，优先按波形特征自适应，不要用会改变黄金样例的全局常数硬推。
11. 单脉冲 TSS 数据只有关断过程，不包含有效的第二脉冲开通、反向恢复、Eon、Err 或开通 di/dt。遇到单脉冲样例时，算法和验证脚本必须进入单脉冲/仅关断口径：只提取并验证 Vdc、Idc、关断 `Delta Vce`、关断 `dv/dt`、关断 `di/dt`、`Ls_off`、`td_off/tf/toff`、Eoff、关断串扰等关断参数；不要把缺失的第二脉冲当成算法失败，也不要为了凑 Eon/Err 人为构造开通窗。
12. TSS Err 兼容性校准：全量验证不能只看 Err 数值闭环，还必须检查放大后卡尺是否贴波形。`Ha` 不再允许使用固定峰后 `400~800ns` 尾窗；必须在 IRM 主峰后、本次开通过程结束前，用滚动局部极差找到主要大振荡已经结束并首次达到低幅稳定标准的稳定窗，并以该窗内 `(max+min)/2` 作为 Irr base 有效值。若该稳定窗为有效负尾段，`Ha` 必须保持带符号负值，不能翻成正的 `abs(Ha)`；正幅值软恢复才用幅值口径显示/交点。`A` 必须是这条 `Ha` 横线与 Irr 曲线在稳定区入口附近的真实交点，不得继续用主峰后第一个下降穿越、固定尾窗交点、后续更小尾窗交点、拟合点或段界点。`Hb` 必须取第二脉冲开通过程/反向恢复段内，对管电压 Vd 主上升沿前的本地 base，`B` 必须卡在 `Hb` 横线与同一段 Vd 主上升沿曲线的真实交点上，这是唯一口径；不能取全局 base、第一脉冲关断过程 base、主沿前基线毛刺、拟合回推点或旧兼容点，也不能保留“主峰前最后一次有效上穿/旧交点贴近则沿用”的兼容分支。此次回归样例：`示例文件/tss格式/KSU2577/SSM1R7PB12B3DTFMMSPP25M4CF0016/SSS/RT/tss/UL_750V_805A_000.tss` 当前必须为 `Ha=-31.671875A`、`A=15.980760us`、`Hb=4.09375V`、`B=15.651653us`、`Err≈9.322471mJ`。`UH_750V_1050A_000.tss`/`UL_750V_1050A_000.tss` 按第 10 条 Err 快照锁定，后续不得改回旧 A/B。
13. Eoff/Eon/Err 损耗卡尺统一专业口径：左侧起点按对应主沿进入有效重叠区确定；右侧终点必须考虑本次开关过程右侧的有效阻尼振荡，落到“本地平台横线与对应波形曲线”的真实插值交点。三类损耗的右侧判定不能混用：Eoff 右侧 B 只能看关断过程 `Ic` 主下降沿与关断本地 `Hb(Ic)` 的回落交点；Eon 右侧 B 只能看开通过程 `Vce` 主下降沿与开通本地 `Hb(Vce)` 的回落交点，且不得把手动拖动竖向光标联动出的 `Hb(Vce)` 显示值写回平台算法；Err 的“从 IRM 尖峰往后第一个达到低幅稳定标准”只适用于反向恢复电流 `Irr` 的 A/Ha，Err 的 Vd-B 仍必须是第二脉冲开通过程/反向恢复段内 Vd 曲线与本地 `Hb(Vd)` 横线的真实交点。右侧震荡是否结束必须看本过程对应波形在本地窗口内的最大值、最小值及峰峰值；严重振荡工况下，平缓区的 `max-min` 必须明显缩小后才能落右侧竖向光标，必要时可对判定用波形做短窗平滑以排除采样毛刺，但最终 A/B 光标仍必须落在原始波形与横向平台线的真实插值交点。不要保留“第一次刚碰平台就截断”“贴近旧交点则沿用”“固定尾窗”“最后显著拖尾”“hold 均值防抖后返回原始小穿越”“B 太近就跳到固定时间点”等旧分支；也不要为了追求更晚光标把稳定平台之后的长尾无限纳入积分。当前实现按损耗类型分别锚定对应波形的对应主沿，再在本过程窗口内用本地平台和有效振荡幅度选择右侧真实交点；光标落点和积分窗口必须使用同一个时间点。
14. 波形界面的交点示意点必须服从参数取值规则，只能画“该参数定义中的竖向光标与对应横向平台/参考线、对应波形曲线”的真实交点，不能因为竖向光标碰到其他通道曲线就额外打点。Eoff 只画 `A=Vce∩Ha(Vce)`、`B=Ic∩Hb(Ic)`；Eon 只画 `A=Ic∩Ha(Ic)`、`B=Vce∩Hb(Vce)`；Err 只画 `A=Irr∩Ha(Irr)`、`B=Vd∩Hb(Vd)`；Trr 必须画 `Irr` 与 `Ha` 的 A/B 两个交点。读数浮窗可以继续显示 A/B 光标处多个相关通道值，但示意点不能复用这个多通道读数列表。
15. Trr 自动结果、默认光标落点、手动拖动后的重算必须共用 `metrics/irr_measure.py` 的同一套核心逻辑：`Ha` 为本次反向恢复过程参考电流线，`A` 是 Irr 进入主恢复瓣时第一次与 `Ha` 相交的真实插值点，`B` 是 Irr 主峰之后第一次回落到 `Ha` 的真实插值点。Trr 只统计主恢复时间，不能因为 Err 能量窗口需要有效尾段就把 Trr 的 B 拖到后续衰减振荡结束；也不得恢复 `reverse_recovery_trr()` 的固定 `5A` 近零阈值、GUI 默认区间的前窗 `mid` 口径，或任何自动表格/默认卡尺/手动卡尺各算一套的分支。
16. 特等重要：凡是修改 TSS 提取、脉冲分段、平台线、损耗窗口、光标落点、积分、斜率、Trr/Err 或任意会影响参数数值的逻辑，完成后必须清理/绕开旧验证缓存，使用 `示例文件` 下全部原始 `.tss` 文件重新跑全量验证训练，并同时覆盖双脉冲与单脉冲样例。不得只跑两个黄金样例、不得只看单个截图、不得拿缓存结果冒充重新验证；全量验证失败、异常数增加、单脉冲被误判为双脉冲、或任一重点样例数值无依据漂移时，不能交付。
